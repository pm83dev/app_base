export const environment = {
  production: true,
  baseURL: '/', // per routing locale
  URL: '/assets/data', // JSON statici serviti dall’app Angular
  API_URL: '/api/', // backend same-origin tramite reverse proxy
};
